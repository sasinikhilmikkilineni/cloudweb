resource "aws_secretsmanager_secret" "mongo_uri" {
  name = var.secret_mongo_uri_name
  tags = local.tags
}
resource "aws_secretsmanager_secret" "jwt_secret" {
  name = var.secret_jwt_secret_name
  tags = local.tags
}
resource "aws_secretsmanager_secret" "paypal_client_id" {
  name = var.secret_paypal_client_id_name
  tags = local.tags
}
# Put your actual values out-of-band (CLI/console/CI) to avoid state exposure.
